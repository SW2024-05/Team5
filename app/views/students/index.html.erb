<h1 class="title">生徒一覧</h1>

<div class="container-ad">
  
  <ol class="stu-list">
    <li>
      <% @Students.each do |s| %>
        <%= link_to s.student_name,"students/#{s.id}", class: "stu-name" %><br/>
      <% end %>
    </li>
  </ol>

</div>
<%= link_to "新規追加" , new_student_path%><br/>